{
	"name": "deep-macos-web-ui",
	"packageManager": "pnpm@10.14.0",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"dev": "vite",
		"preview": "vite preview",
		"check": "pnpm build && pnpm lint && pnpm format",
		"build": "tsc -b && vite build",
		"lint": "exec biome lint --write",
		"format": "exec biome format --write"
	},
	"dependencies": {
		"preact": "^10.27.0"
	},
	"devDependencies": {
		"@biomejs/biome": "2.2.0",
		"@preact/preset-vite": "^2.10.2",
		"typescript": "~5.8.3",
		"vite": "^7.1.2"
	},
	"engines": {
		"node": "22"
	}
}
